CREATE TABLE IF NOT EXISTS logQuebraQlik (
    evento TEXT NOT NULL,
    statusQuebra TEXT NOT NULL,
    codigoStatus TEXT NOT NULL
    dataQuebra DATE NOT NULL,
    IDCompleto TEXT NOT NULL,
    caminhoLog TEXT NOT NULL
);